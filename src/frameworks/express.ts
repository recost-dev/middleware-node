/**
 * Express adapter for @ecoapi/node middleware.
 * Logic TBD. Scaffold only.
 */

// TODO: import express types (peer dependency) and expose createEcoMiddleware()

export function createExpressMiddleware(): never {
  throw new Error("Not implemented");
}
