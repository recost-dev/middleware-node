/**
 * Fastify adapter for @ecoapi/node middleware.
 * Logic TBD. Scaffold only.
 */

// TODO: import fastify types (peer dependency) and expose createEcoPlugin()

export function createFastifyPlugin(): never {
  throw new Error("Not implemented");
}
